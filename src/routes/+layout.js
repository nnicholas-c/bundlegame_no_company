// Disable SSR for the entire app since everything runs client-side
export const ssr = false;
// Enable client-side routing
export const csr = true;
// Prerender all routes as static HTML
export const prerender = true;
